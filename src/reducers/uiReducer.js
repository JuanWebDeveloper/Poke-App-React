const initialState = {
  errorMessage: '',
  loadign: false,
  withMistakes: false,
  validateField: '',
};

export const uiReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
